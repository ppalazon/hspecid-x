package:
  name: HSpecID-X
  authors: ["Pablo Palazon <pablo.palazon@alu.uclm.es>"]

dependencies:
  common_cells:
    {
      git: "https://github.com/pulp-platform/common_cells.git",
      version: "1.38.0",
    }

  register_interface:
    {
      git: "https://github.com/pulp-platform/register_interface.git",
      version: "0.4.5",
    }

  obi: { git: "https://github.com/pulp-platform/obi.git", version: "0.1.7" }

# Set to true to avoid updating the dependencies automatically
frozen: false

sources:
  - hw/src/**/rtl/*.sv

workspace:
  checkout_dir: hw/deps
